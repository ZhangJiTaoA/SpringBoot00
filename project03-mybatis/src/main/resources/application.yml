# datasource
spring:
  datasource:
    url: jdbc:mysql:/用户名/ip：端口/springboot?serviceTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
# mybatis
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml  # mapper的映射文件路径
  type-aliases-package: com.zjt.project03mybatis.domain
#  config-location:   # 指定mybatis的核心配置文件